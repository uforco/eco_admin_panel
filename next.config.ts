import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images:{
    domains: ['github.com', 'lh3.googleusercontent.com', 'sharifdev.vercel.app']
  }
};

export default nextConfig;
